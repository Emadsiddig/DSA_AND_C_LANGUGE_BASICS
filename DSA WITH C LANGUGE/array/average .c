#include<stdio.h>

int average(int arr[],int n){
    int sum=0;

    for(int i=0;i<n;i++){
        sum+=arr[0];
    }
    int avg=sum/2
    return avg;
}
int main()
{
    int n;
    printf("entrer the  number of element in arr");
    scanf("%d",&n);
    int arr[n];
    printf("enter the number of arr");
    for(int i=0;i<n;i++);{
    scanf("%d",&arr{i});
   }
 average(arr[n],n);

return 0;
}
